<?xml version="1.0" encoding="utf-8"?>
<!--
  v2.0 — 患者端主页 + 4 Tab 底部导航
  设计语言：暖 Indigo · 大圆角 · 深度阴影 · 柔和背景
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinator_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

    <!-- ═══════ 连接状态横幅 ═══════ -->
    <LinearLayout
        android:id="@+id/banner_connection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp" android:layout_marginEnd="16dp"
        android:layout_marginTop="10dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="14dp" android:paddingVertical="12dp"
        android:background="@drawable/bg_message_bubble"
        android:elevation="1dp">
        <View android:layout_width="8dp" android:layout_height="8dp"
            android:background="@drawable/bg_step_active" android:id="@+id/dot_status" />
        <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:orientation="vertical"
            android:layout_marginStart="10dp">
            <TextView android:id="@+id/tv_connection_status"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="等待连接" android:textSize="14sp"
                android:textColor="@color/status_waiting_text"
                android:fontFamily="sans-serif-medium" />
            <TextView android:id="@+id/tv_room_number"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="房间 ----" android:textSize="11sp"
                android:textColor="@color/text_tertiary"
                android:layout_marginTop="1dp" />
        </LinearLayout>
    </LinearLayout>

    <!-- ═══════ Fragment 容器 ═══════ -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- ═══════ 底部导航 ═══════ -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        android:elevation="8dp"
        app:menu="@menu/bottom_nav_menu"
        app:labelVisibilityMode="labeled"
        app:itemIconTint="@color/bottom_nav_icon_tint_selector"
        app:itemTextColor="@color/bottom_nav_text_color_selector"
        app:itemTextAppearanceActive="@style/BottomNavLabel"
        app:itemTextAppearanceInactive="@style/BottomNavLabel" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
